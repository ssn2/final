obj-m += chr_drv.o

# Совместимость с gcc новее, чем у сборки ядра (см. Makefile).
ccflags-y += -mfunction-return=keep

chr_drv-y += src/chr_drv.o 
chr_drv-y += src/chr_drv_params.o 
chr_drv-y += src/chr_drv_main.o 
